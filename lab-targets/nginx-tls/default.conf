server {
    listen 443 ssl;
    server_name lab.local;

    ssl_certificate     /etc/nginx/ssl/selfsigned.crt;
    ssl_certificate_key /etc/nginx/ssl/selfsigned.key;

    location / {
        return 200 "Lab web server (TLS)\n";
        add_header Content-Type text/plain;
    }
}
